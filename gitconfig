[alias]
    s = status
    au = add -u
    br = branch
    cm = commit
    cma = commit --amend
    co = checkout
    cob = checkout -b
    logo = log --oneline
    logg = log --oneline --graph --decorate
    df = diff
    dfw = diff --word-diff=color
    dfc = diff --cached

[user]
    name = Alex Chew
    email = alex@alexchew.net

[push]
    default = simple
