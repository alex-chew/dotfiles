#!/usr/bin/env bash

(
  cd ~/dotfiles
  color=$(find cfg -maxdepth 1 -type f -printf "%f\n" \
    | rofi -dmenu -i -no-custom -p "colorscheme: ")
  [[ -n "$color" ]] && ./render "cfg/$color" && ./postdeploy
)

