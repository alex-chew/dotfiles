#!/usr/bin/env bash

# full text
perc=$(free -m | awk '/^Mem/ { printf "%.0f", (1 - $7/$2) * 100 }')
echo "$perc"

# short text
echo

# color
if (( perc >= 80 )); then
  echo "{{ i3.urgent }}"
elif (( perc >= 50 )); then
  echo "{{ i3.warning }}"
else
  echo "{{ i3.fg }}"
fi

