#!/usr/bin/env bash
source $(dirname $0)/_colors

state=$(nmcli --terse --fields GENERAL.CONNECTION dev show eth0 | cut -d ':' -f 2)

# full text
echo "$(icon ) $state"
# short text
echo

if [[ "$state" == "--" ]]; then
  # color
  echo ${base03}
else
  # color
  echo ${base07}
fi

